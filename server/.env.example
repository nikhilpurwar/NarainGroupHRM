# Server Environment Variables

# Port the server runs on
PORT=5100

# MongoDB Connection URL
MONGO_URL=mongodb+srv://user:password@cluster.mongodb.net/iudo_hrm?retryWrites=true&w=majority

# JWT Secret for token signing (use a strong random string)
JWT_SECRET=your-secret-key-here-change-in-production

# Node environment: development, production
NODE_ENV=development

# CORS Origins (comma-separated) - leave empty to allow all origins in development
# Example for Render: CORS_ORIGINS=https://naraingrouphrm-o8al.onrender.com
# Example for localhost: CORS_ORIGINS=http://localhost:5173,http://localhost:3000
CORS_ORIGINS=

# Render specific: use internal URLs
# MONGO_URL=your-mongodb-url
# JWT_SECRET=your-production-secret
# PORT=5100
# NODE_ENV=production
# CORS_ORIGINS=https://naraingrouphrm-o8al.onrender.com
