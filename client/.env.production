# For production on Render, use empty URL so frontend and backend are on same domain
# Frontend components use: ${VITE_API_URL}/api/... so this becomes /api/...
VITE_API_URL=https://naraingrouphrm.onrender.com
